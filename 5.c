// Arrange four nines(9) and a one(1) and only one 
// mathematical symbol to make it equal to 100 ? 

#include <stdio.h>

int main() {
    int result = 99 + 9 / 9;

    printf("Arrangement: 99 + 9 / 9 = %d\n", result);

    return 0;
}
